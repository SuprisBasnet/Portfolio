# Project screenshots

Drop the final project screenshots in this folder when ready, for example:

- `flight-delay.png`
- `health-focus.png`

The Work section is already structured around the two current projects. Repository links can be updated directly in `src/components/Work.tsx` once the GitHub URLs are available.
